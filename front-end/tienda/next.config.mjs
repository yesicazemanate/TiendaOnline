/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['m.media-amazon.com', 'i.pinimg.com', 'ae01.alicdn.com','lostreselefantes.com.co','pieers.com'],
      },
};

export default nextConfig;
